// This component is a placeholder as we no longer support theme switching
import { FC } from 'react';

interface ThemeToggleProps {
  mobile?: boolean;
}

const ThemeToggle: FC<ThemeToggleProps> = () => {
  // Return null since we removed theme toggling
  return null;
};

export default ThemeToggle;